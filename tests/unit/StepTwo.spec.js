import { shallowMount } from "@vue/test-utils";
import StepTwo from "@/src/views/StepTwo/index.vue";

describe("StepTwo.vue", () => {
  it("Should show proper price", async () => {
    // const wrapper = shallowMount(StepTwo);
    // const nameInput = wrapper.find("input.name");
    // await nameInput.setValue("Chheung");
    // const ageInput = wrapper.find("input.age");
    // await ageInput.setValue("10");
    // const locationSelect = wrapper.find("select.location");
    // await locationSelect.at(0).instance().selected;
    // const radioInput = wrapper.find('input[name="package"]');
    // await radioInput.setChecked(true);
    // expect(wrapper.find('p[class="premium-price"]')).toMatch(
    //   "Your premium is 100 HKD"
    // );
  });
});
